# Release 0.0.14

- Publishes the unified browser lifecycle API from PR #24 for ws-edn and Calcium
  Workflow consumers.
- Keeps `deps.cirru` and `package.json` on the same release version.
- This is a package release only; no snapshot/source behavior changes are made by
  this commit.
