{
  "dependencies": {
    "@calcit/procs": "^0.5.16"
  },
  "scripts": {
    "bundle-server": "esbuild ./server.js --bundle --outfile=js-out/bundle.js --platform=node",
    "watch-server": "cr --emit-js",
    "watch-page": "cr --emit-js --emit-path out-page/ --init-fn cumulo-util.client/main! --reload-fn cumulo-util.client/reload!"
  },
  "devDependencies": {
    "shortid": "^2.2.16",
    "vite": "^2.7.13",
    "ws": "^8.4.2"
  },
  "version": "0.0.2"
}
