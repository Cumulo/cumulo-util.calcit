{
  "version": "0.0.8",
  "dependencies": {
    "@calcit/procs": "^0.12.56"
  },
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "build": "yarn compile-page && vite build",
    "compile-page": "cr js",
    "compile-server": "cr --entry server js",
    "dev": "vite",
    "test": "yarn compile-page && node tests/browser-lifecycle.mjs",
    "watch-page": "cr -w js",
    "watch-server": "cr -w --entry server js"
  },
  "devDependencies": {
    "vite": "^8.0.5"
  }
}
