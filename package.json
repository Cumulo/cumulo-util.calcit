{
  "version": "0.0.6",
  "dependencies": {
    "@calcit/procs": "^0.8.0-a3",
    "nanoid": "^4.0.2"
  },
  "scripts": {
    "watch-server": "cr --emit-js --entry server",
    "watch-page": "cr --emit-js --emit-path out-page/"
  },
  "devDependencies": {
    "vite": "^4.4.9"
  }
}
