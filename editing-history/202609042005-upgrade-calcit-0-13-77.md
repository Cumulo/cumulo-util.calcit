# Upgrade to Calcit 0.13.77 / 升级到 Calcit 0.13.77

UTC: 2026-09-04T20:05:37Z

## English

- Align the project compiler and JavaScript runtime package to exact 0.13.77
  while preserving module version 0.0.16 and all scheduling/lifecycle APIs.
- Keep the dependency graph empty and verify it with strict Caps plus exact
  toolchain checks.
- Harden the validation workflow with reviewed immutable action revisions,
  read-only permissions, non-persisted checkout credentials, a bounded timeout,
  and cancellation of superseded runs.
- Validate canonical Snapshot formatting, both page and server entries, the
  reviewed quality baseline, dynamic dispatch, browser lifecycle behavior,
  realtime scheduling primitives, JavaScript generation, and the Vite build.
- Regenerate the native v2 quality baseline with Calcit 0.13.77, lowering the
  reviewed unsafe-coerce budget from 5 to 4 without increasing any metric.
- Keep the Node file-helper block explicitly non-executable because it contains
  consumer placeholders; executable README blocks pass the documentation gate.

## 中文

- 将 compiler 与 JavaScript runtime package 精确对齐到 0.13.77，同时保持模块
  版本 0.0.16 及 scheduling/lifecycle API 不变。
- 保持依赖图为空，并通过严格 Caps 与精确工具链校验确认。
- 使用已审阅的不可变 Action revision、只读权限、非持久化 checkout 凭据、超时
  与取消过期运行来加固验证 workflow。
- 验证 canonical Snapshot、page/server 双 entry、既有质量 baseline、动态分派、
  浏览器生命周期、实时调度原语、JavaScript 生成和 Vite 构建。
- 使用 Calcit 0.13.77 重新生成原生 v2 quality baseline，将已审阅的
  unsafe-coerce 上限从 5 降到 4，其他指标均未增长。
- Node 文件 helper 代码块包含消费者占位符，明确标为不可执行；其余 README
  可执行代码块通过文档检查。
