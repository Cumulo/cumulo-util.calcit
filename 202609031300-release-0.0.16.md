# Release 0.0.16 / 发布 0.0.16

- Publish the typed `page-visible?` reuse from #29 so legacy `visibility-heartbeat` no longer compares an opaque DOM value under Calcit 0.13.76.
- Align the Calcit CLI and `@calcit/procs` runtime at the released 0.13.76 version.
- 发布 #29 中对类型化 `page-visible?` 的复用，避免 legacy `visibility-heartbeat` 在 Calcit 0.13.76 下比较不透明 DOM 值。
- 将 Calcit CLI 与 `@calcit/procs` runtime 对齐到已发布的 0.13.76。
